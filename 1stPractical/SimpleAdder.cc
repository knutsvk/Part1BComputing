#include <iostream>
using namespace std; 

int main()
{
    double a, b, product;

    cout << "Enter two numbers to be multiplied together: " << endl;
    cin >> a >> b; 

    product = a * b; 
    cout << "The product is " << product << endl;

    return 0;
}
